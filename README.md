## 실행방법

이 프로젝트는 편리한 실행을 위해 `Makefile`을 제공합니다.

### 1. 로컬 환경에서 실행 (Ruby, Node, Postgres 설치 필요)
```bash
make setup  # 초기 의존성 설치 및 데이터베이스 세팅
make dev    # 프론트엔드와 백엔드 서버 동시 실행
```

### 2. 도커로 실행 (Docker 설치 필요)
```bash
make docker-up    # 컨테이너 빌드 및 실행
make docker-down  # 컨테이너 중지
```

### 3. 관리자 정보 (Admin UI, seed 데이터 기준)
- **주소**: http://localhost:3000/admin
- **계정**: admin@example.com / password123

### 4. 프론트 정보
- **주소**: http://localhost:8080


### 5. 간략 구조도
┌─────────────────────────────────────────────────────────────────────────┐
│                              브라우저                                   │
│  ┌──────────────┐                              ┌──────────────────────┐ │
│  │  마이크 입력 │         ──PCM 16kHz──▶       │   오디오 재생        │ │
│  │(AudioWorklet)│                              │ (Web Audio API)      │ │
│  └──────────────┘                              └──────────────────────┘ │
└─────────────────────────────────────────────────────────────────────────┘
                 WebSocket            │ ▲
                                    ▼ │
┌─────────────────────────────────────────────────────────────────────────┐
│                           백엔드 서버 (rails)                           │
│                                                                         │
│        Audio ──▶ sttStream ──▶ LLM API ──▶ ttsStream ──▶ Events         │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
                    │                                   │
                    ▼                                   ▼
            ┌──────────────┐                    ┌──────────────┐
            │  AssemblyAI  │                    │   Cartesia   │
            │  (STT API)   │                    │  (TTS API)   │
            └──────────────┘                    └──────────────┘
